machine:
  environment:
    DOCKER_REPOSITORY: zirra/libpostal-rest

  services:
    - docker



test:


  override:
    - echo 'test'

deployment:
  eb-master:
    branch: master
    commands:
#      - ./.deployment/circle/dockerhub_push.sh $DOCKER_REPOSITORY $CIRCLE_BRANCH
      - ./.deployment/circle/eb_deploy.sh $DOCKER_REPOSITORY $CIRCLE_BRANCH libpostal-rest
