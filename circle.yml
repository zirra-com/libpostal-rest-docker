machine:
  environment:
    DOCKER_REPOSITORY: zirra/libpostal-rest
    UNIQUEID: $CIRCLE_BRANCH-$CIRCLE_SHA1

  services:
    - docker



test:


  override:
    - echo 'test'

deployment:
  eb-master:
    branch: master
    commands:
      - ./.deployment/circle/dockerhub_push.sh $DOCKER_REPOSITORY $UNIQUEID
      - ./.deployment/circle/eb_deploy.sh $DOCKER_REPOSITORY $UNIQUEID libpostal-rest
